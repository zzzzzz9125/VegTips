{
  "devDependencies": {
    "@types/node": "^25.0.3",
    "gh-pages": "^6.3.0",
    "pinyin-pro": "^3.22.0",
    "vitepress": "^2.0.0-alpha.15"
  },
  "scripts": {
    "prebuild": "node scripts/copy-locale-pages.mjs",
    "docs:dev": "npm run prebuild && vitepress dev docs",
    "docs:build": "npm run prebuild && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:deploy": "npm run docs:build && gh-pages -d docs/.vitepress/dist -t true"
  }
}
