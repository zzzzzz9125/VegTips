---
layout: doc
---

@en # VEGAS Pro Script API Browser
@zh # VEGAS Pro 脚本 API 浏览器
@zh-hant # VEGAS Pro 指令碼 API 瀏覽器
@ja # VEGAS Pro スクリプト API ブラウザー
@ko # VEGAS Pro 스크립트 API 브라우저
@de # VEGAS Pro Skript-API-Browser
@fr # Navigateur Script API de VEGAS Pro
@ru # Браузер Script API для VEGAS Pro

@en - Supports filtering by `Kind` and by class under `ScriptPortal.Vegas`.
@zh - 支持按 `Kind` 与 `ScriptPortal.Vegas` 下的类进行筛选。
@zh-hant - 支援依 `Kind` 與 `ScriptPortal.Vegas` 下的類別篩選。
@ja - `Kind` と `ScriptPortal.Vegas` 配下のクラスで絞り込み可能です。
@ko - `Kind` 및 `ScriptPortal.Vegas` 하위 클래스 기준 필터링을 지원합니다.
@de - Filterung nach `Kind` und Klassen unter `ScriptPortal.Vegas`.
@fr - Filtrage par `Kind` et par classe sous `ScriptPortal.Vegas`.
@ru - Поддерживается фильтрация по `Kind` и по классам в `ScriptPortal.Vegas`.

@en - Supports diff mode to compare two versions and show detailed changes.
@zh - 支持双版本差异对比，并显示具体变更内容。
@zh-hant - 支援雙版本差異比較，並顯示具體變更內容。
@ja - 2 バージョンの差分比較と、具体的な変更内容の表示に対応します。
@ko - 두 버전 비교와 상세 변경 내용 확인을 지원합니다.
@de - Vergleich von zwei Versionen mit detaillierter Differenzanzeige.
@fr - Comparaison de deux versions avec affichage détaillé des différences.
@ru - Поддерживается сравнение двух версий с детальным выводом различий.

<ScriptApiExplorer />
