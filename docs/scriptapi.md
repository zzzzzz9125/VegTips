---
layout: doc
---

@@@en
# VEGAS Pro Script API Browser

Browse and compare Script API XML across VEGAS Pro versions.

- Supports filtering by `Kind` and by class under `ScriptPortal.Vegas`.
- Supports diff mode to compare two versions and show detailed changes.
@@@zh
# VEGAS Pro 脚本 API 浏览器

用于浏览并对比不同 VEGAS Pro 版本的 Script API XML。

- 支持按 `Kind` 与 `ScriptPortal.Vegas` 下的类进行筛选。
- 支持双版本差异对比，并显示具体变更内容。
@@@zh-hant
# VEGAS Pro 指令碼 API 瀏覽器

用於瀏覽並比較不同 VEGAS Pro 版本的 Script API XML。

- 支援依 `Kind` 與 `ScriptPortal.Vegas` 下的類別篩選。
- 支援雙版本差異比較，並顯示具體變更內容。
@@@ja
# VEGAS Pro スクリプト API ブラウザー

VEGAS Pro の各バージョンにある Script API XML を閲覧・比較できます。

- `Kind` と `ScriptPortal.Vegas` 配下のクラスで絞り込み可能です。
- 2 バージョンの差分比較と、具体的な変更内容の表示に対応します。
@@@ko
# VEGAS Pro 스크립트 API 브라우저

VEGAS Pro 버전별 Script API XML을 조회하고 비교할 수 있습니다.

- `Kind` 및 `ScriptPortal.Vegas` 하위 클래스 기준 필터링을 지원합니다.
- 두 버전 비교와 상세 변경 내용 확인을 지원합니다.
@@@de
# VEGAS Pro Skript-API-Browser

Zum Durchsuchen und Vergleichen der Script-API-XML-Dateien verschiedener VEGAS Pro Versionen.

- Filterung nach `Kind` und Klassen unter `ScriptPortal.Vegas`.
- Vergleich von zwei Versionen mit detaillierter Differenzanzeige.
@@@fr
# Navigateur Script API de VEGAS Pro

Permet d'afficher et de comparer les fichiers XML de la Script API selon les versions de VEGAS Pro.

- Filtrage par `Kind` et par classe sous `ScriptPortal.Vegas`.
- Comparaison de deux versions avec affichage détaillé des différences.
@@@ru
# Браузер Script API для VEGAS Pro

Страница для просмотра и сравнения XML Script API между версиями VEGAS Pro.

- Поддерживается фильтрация по `Kind` и по классам в `ScriptPortal.Vegas`.
- Поддерживается сравнение двух версий с детальным выводом различий.
@@@

<ScriptApiExplorer />
